{
  "name": "mcp-server-typescript",
  "version": "1.0.0",
  "description": "MCP server with TypeScript and auto-build",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch & nodemon --watch dist dist/index.js",
    "start": "node dist/index.js",
    "serve": "npm run build && npm start"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^20.0.0",
    "nodemon": "^3.1.10",
    "typescript": "^5.0.0"
  }
}
